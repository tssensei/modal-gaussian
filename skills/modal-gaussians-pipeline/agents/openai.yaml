interface:
  display_name: "Modal Gaussians Pipeline"
  short_description: "Generate validated 3D modes without fitting coordinates"
  default_prompt: "Use $modal-gaussians-pipeline to obtain validated 3D modes at my requested frequencies, fixing and resuming in-scope failures automatically; stop without fitting modal coordinates or launching Viser."
