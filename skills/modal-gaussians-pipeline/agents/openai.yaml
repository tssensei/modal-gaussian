interface:
  display_name: "Modal Gaussians Pipeline"
  short_description: "Generate 3D modes from cached SEA-RAFT spectra"
  default_prompt: "Use $modal-gaussians-pipeline to obtain 3D modes at my requested frequencies from SEA-RAFT and cached spectra, fixing and resuming in-scope failures; stop without experiment validation, modal-coordinate fitting or launching Viser."
