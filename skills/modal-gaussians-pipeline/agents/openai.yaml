interface:
  display_name: "Modal Gaussians Pipeline"
  short_description: "Run and validate the pipeline to a Viser-ready result"
  default_prompt: "Use $modal-gaussians-pipeline to run my prepared experiment to validated Viser-ready artifacts with live logs, autonomously diagnosing, repairing, verifying, and resuming after failures within the agreed scope; provide the Viser launch command without launching it."
